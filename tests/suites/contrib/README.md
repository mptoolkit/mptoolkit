# Contrib Suites

These suites exercise `models/contrib` generators. They are useful smoke tests
when working on contrib models, but contrib models are outside the supported
default integration suite set and these suites are not CI blockers.
